# Database host (e.g. localhost or a remote IP)
DB_HOST=localhost

# MySQL username
DB_USER=root

# MySQL password — leave empty if no password is set
DB_PASSWORD=

# Name of the MySQL database
DB_NAME=fundzola

# MySQL port (default: 3306)
DB_PORT=3306

# Port the Express server will listen on
PORT=5000
